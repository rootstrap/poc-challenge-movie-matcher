import React from 'react';

import View from 'components/View';

import styles from './styles';

const ListItemSeparator: React.FunctionComponent = ({}) => {
  return <View style={styles.separator} />;
};

export default ListItemSeparator;
